TODO list for today

agent须知：agent需要先README.md，并且查看其中引用的编码规范等，获得context，然后再进行工作

- 1 为了效率问题， 整个图像， 相机的id应该重新编码， 只需要在开始记录映射表，结束恢复即可，为此我们应该在构建Attask，导出各种参数的时候来构建映射表，结束AT的时候再映射回去就行。 sfm在实现上， 不要unordered_map,而是用vector,内存和速度都会更快，也会适配GPU
  - **已实现**：IdMapping（id_mapping.h）、从 image list 构建映射（isat_intrinsics.h `build_id_mapping_from_image_list`）；MultiCameraSetup 改为 vector（image_to_camera、cameras）；view_graph_loader / run_initial_pair_loop 支持 id_mapping（pair 转内部索引、文件路径用原始 ID）；isat_incremental_sfm 构建映射与向量 setup，输出时用映射还原为原始 image_id。
- 2 实现解析求导的BA，rotation需要参数化成四元数。
  已经实现
- 3 修改之前留下的各种错误：
  - 3.1 incremental_sfm.cpp: line 378 不能假设两个K是一致的， 
  - 3.2 run_initial_pair_loop 在这里， 我们应该保证选择出来的pair，三角化优化等等是能成功的，否则应该继续选择pair，所以英嘎是一个循环。 
  - 3.3 incremental_sfm_engine,line 75-77,这里都是伪造的， 内参数应该直接拿到。 没有什么reference 内参。 我们就是提供好了内参。我们要系统考虑问题。 
- 4 整个sfm的框架是混乱的。 下面进行以下改造
  - 4.1 重构整个代码。 
  - 4.2 sfm流程去掉idmapping。从导出数据的那一刻，就完成了idmapping。 然后从特征开始，文件名字就是index。 后面所有的算法依赖的都是index。 这样就去掉了idmapping，最后由算法外层负责转换index和id之间的关系。
  - 4.3 整体概念必须贯彻已知intrinsic的概念，所以每个image对应的intrinsics的index是已知的，直接数组索引就好了，不必再构建了，应该在一开始就构建好。 其实在project管理的时候， 就是一个intrinsc对应多个image，所以一开始就构建好，后面算法必须拿到。 
  - 4.5 为此，很多算法模块不必要的设计需要修改。 整个sfm算法需要以下改正。 1 减少IO，View graph，track应该构建一次，现在没有idmapping应该更高效。 后面依赖的地方不希望再有io，直接内存。 2 初始图像的选择需要有循环策略， 不能完全信任geo的信息。 需要进入ba,ba以后需要评估退化，只要选择失败了， 应该暂时屏蔽选择的两幅pair，接着循环，直到无法选择或者整体失败。 